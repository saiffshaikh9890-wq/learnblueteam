{
  "name": "learnblueteam",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "postinstall": "node -e \"try{const f=require.resolve('fork-ts-checker-webpack-plugin/lib/ForkTsCheckerWebpackPlugin');console.log('fork-ts-checker ok')}catch(e){console.log('skipping fork-ts-checker patch')}\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": "18.x"
  },
  "overrides": {
    "fork-ts-checker-webpack-plugin": ">=4.0.0 <5.0.0"
  }
}